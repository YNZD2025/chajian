<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nian.yinianzhida.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.nian.yinianzhida.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 根据ID查询消息 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM chat_message WHERE id = #{id}
    </select>

    <!-- 根据会话ID查询所有消息 -->
    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT * FROM chat_message WHERE session_id = #{sessionId} ORDER BY created_at ASC
    </select>

    <!-- 插入新消息 -->
    <insert id="insert" parameterType="com.nian.yinianzhida.entity.ChatMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (
            session_id,
            role,
            content,
            created_at
        ) VALUES (
            #{sessionId},
            #{role},
            #{content},
            NOW()
        )
    </insert>

    <!-- 批量插入消息 -->
    <insert id="batchInsert">
        INSERT INTO chat_message (session_id, role, content, created_at)
        VALUES
        <foreach collection="messages" item="message" separator=",">
            (#{message.sessionId}, #{message.role}, #{message.content}, NOW())
        </foreach>
    </insert>

    <!-- 删除会话的所有消息 -->
    <delete id="deleteBySessionId">
        DELETE FROM chat_message WHERE session_id = #{sessionId}
    </delete>

</mapper>
