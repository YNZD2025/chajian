<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nian.yinianzhida.mapper.AdminMapper">

    <!-- 结果映射 -->
    <resultMap id="AdminResultMap" type="com.nian.yinianzhida.entity.Admin">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 根据用户名查询管理员 -->
    <select id="findByUsername" resultMap="AdminResultMap">
        SELECT id, username, password, real_name, phone, role, status,
               last_login_time, created_at, updated_at
        FROM admin
        WHERE username = #{username}
    </select>

    <!-- 根据ID查询管理员 -->
    <select id="findById" resultMap="AdminResultMap">
        SELECT id, username, password, real_name, phone, role, status,
               last_login_time, created_at, updated_at
        FROM admin
        WHERE id = #{id}
    </select>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime">
        UPDATE admin
        SET last_login_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 插入管理员 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (username, password, real_name, phone, role, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{role}, #{status}, NOW(), NOW())
    </insert>

    <!-- 更新管理员信息 -->
    <update id="update">
        UPDATE admin
        SET username = #{username},
            real_name = #{realName},
            phone = #{phone},
            role = #{role},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除管理员 -->
    <delete id="deleteById">
        DELETE FROM admin
        WHERE id = #{id}
    </delete>

</mapper>
