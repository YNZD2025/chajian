# 🎉 插件最终配置方案（已解决局域网访问问题）

## 📊 当前环境

### 开发电脑
- **IP地址**：`192.168.1.144`
- **后端**：`localhost:8080` ✅ 运行中
- **前端**：`localhost:3000` ✅ 运行中
- **内网穿透**：`http://z6467e53.natappfree.cc` → `localhost:8080` ✅ 运行中

### 插件电脑
- **IP地址**：`192.168.1.92`（从日志推测）
- **与开发电脑**：✅ 在同一局域网

---

## ✅ 已完成的修改

### 1. 前端API配置（已修改）

**文件**：`front/src/utils/request.js`

```javascript
const request = axios.create({
  // 使用内网穿透地址作为默认值
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://z6467e53.natappfree.cc',
  timeout: 30000
})
```

**效果**：
- ✅ 从插件电脑访问 `http://192.168.1.144:3000` 时，前端会直接请求 `http://z6467e53.natappfree.cc`
- ✅ 二维码生成、登录、所有API调用都会使用公网地址
- ✅ 插件电脑无需运行后端

### 2. Vite代理配置（已恢复）

**文件**：`front/vite.config.js`

```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',  // 本地开发用本地后端
    changeOrigin: true,
    secure: false
  }
}
```

**说明**：
- 代理只在访问 `localhost:3000` 时生效
- 通过局域网IP访问时，代理不生效，直接使用 `baseURL`

---

## 🚀 插件配置代码

### 在插件的 `config.js` 中使用：

```javascript
const config = {
  // 后端API地址（使用公网地址）
  API_BASE_URL: 'http://z6467e53.natappfree.cc/api/autofill/',
  API_AUTH_URL: 'http://z6467e53.natappfree.cc/api/plugin/',
  
  // 前端登录页面（使用局域网IP）
  WEB_LOGIN_URL: 'http://192.168.1.144:3000/login?from=plugin',
  
  // 域名
  DOMAIN: 'http://192.168.1.144:3000'
};

export default config;
```

---

## 🧪 测试步骤

### 步骤1：在插件电脑测试前端访问

在插件电脑的浏览器打开：
```
http://192.168.1.144:3000/login?from=plugin
```

**预期结果**：
- ✅ 显示完整的登录页面（不是JSON）
- ✅ 显示二维码
- ✅ 页面提示"插件登录模式：登录后将自动返回插件"

---

### 步骤2：检查二维码链接

在登录页面，右键点击二维码 → 审查元素：

**预期结果**：
- 二维码的URL应该包含：`http://z6467e53.natappfree.cc`
- 而不是 `localhost:8080`

---

### 步骤3：测试完整登录流程

在插件中调用：
```javascript
openLoginWindow();
```

**预期流程**：
1. 打开新窗口：`http://192.168.1.144:3000/login?from=plugin`
2. 显示二维码（公网地址）
3. 用手机微信扫码
4. 登录成功后，前端生成 loginToken
5. 通过 PostMessage 发送给插件
6. 插件用 loginToken 换取正式 token
7. 登录窗口自动关闭

---

## 📡 工作原理

### 访问流程图

```
插件电脑浏览器
     ↓
访问: http://192.168.1.144:3000/login
     ↓
前端页面加载
     ↓
前端发起API请求 (生成二维码)
     ↓
请求地址: http://z6467e53.natappfree.cc/api/auth/qrcode/generate
     ↓
natapp内网穿透
     ↓
转发到: localhost:8080 (开发电脑后端)
     ↓
后端返回二维码数据
     ↓
前端显示二维码
```

**关键点**：
- ✅ 前端通过局域网IP访问（`192.168.1.144:3000`）
- ✅ API请求通过公网地址（`z6467e53.natappfree.cc`）
- ✅ 插件电脑无需运行任何服务

---

## 🔍 调试技巧

### 1. 检查前端API请求地址

在插件电脑的浏览器，打开开发者工具 → Network 面板：

**正确的请求**：
```
Request URL: http://z6467e53.natappfree.cc/api/auth/qrcode/generate
Status: 200 OK
```

**错误的请求**（如果看到这个说明配置没生效）：
```
Request URL: http://localhost:8080/api/auth/qrcode/generate
Status: ERR_CONNECTION_REFUSED
```

---

### 2. 检查前端是否启动

在开发电脑运行：
```powershell
netstat -ano | Select-String ":3000" | Select-String "LISTENING"
```

**预期结果**：
```
TCP    0.0.0.0:3000           0.0.0.0:0              LISTENING       30280
TCP    [::]:3000              [::]:0                 LISTENING       30280
```

---

### 3. 检查后端是否运行

在开发电脑运行：
```powershell
netstat -ano | Select-String ":8080" | Select-String "LISTENING"
```

**预期结果**：
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING       24188
```

---

### 4. 检查内网穿透

在任意浏览器访问：
```
http://z6467e53.natappfree.cc/api/plugin/validate-token
```

**预期结果**（JSON响应，说明后端可访问）：
```json
{"success":false,"message":"未登录或登录已过期"}
```

---

## ⚠️ 注意事项

### 1. 内网穿透保持运行

内网穿透必须保持运行，否则插件无法访问后端API：
- ✅ natapp 正在运行
- ✅ 映射端口：8080
- ✅ 公网地址：`http://z6467e53.natappfree.cc`

### 2. 前端必须运行

插件电脑访问前端登录页面需要前端服务运行：
- ✅ 前端运行在：`localhost:3000`
- ✅ 通过局域网访问：`http://192.168.1.144:3000`

### 3. 防火墙设置

确保开发电脑的防火墙允许3000端口访问：
```powershell
# 检查防火墙规则（如需要）
netsh advfirewall firewall show rule name=all | Select-String "3000"
```

### 4. 域名变化处理

如果natapp重启导致域名变化：
1. 更新 `front/src/utils/request.js` 的 baseURL
2. 更新 `application-dev.yml` 的 `app.domain`
3. 重启前端和后端
4. 更新插件的 `config.js`

---

## 📝 完整配置文件汇总

### 1. 后端配置

**文件**：`yinianzhida/src/main/resources/config/application-dev.yml`

```yaml
app:
  base-url: http://z6467e53.natappfree.cc
  domain: http://z6467e53.natappfree.cc

wechat:
  callback:
    url: http://z6467e53.natappfree.cc/auth/wechat/callback
```

---

### 2. 前端API配置

**文件**：`front/src/utils/request.js`

```javascript
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://z6467e53.natappfree.cc',
  timeout: 30000
})
```

---

### 3. 插件配置

**文件**：插件项目的 `config.js`

```javascript
const config = {
  API_BASE_URL: 'http://z6467e53.natappfree.cc/api/autofill/',
  API_AUTH_URL: 'http://z6467e53.natappfree.cc/api/plugin/',
  WEB_LOGIN_URL: 'http://192.168.1.144:3000/login?from=plugin',
  DOMAIN: 'http://192.168.1.144:3000'
};
```

---

## ✅ 最终检查清单

部署前确认：

- [x] 后端运行在 `localhost:8080`
- [x] 前端运行在 `localhost:3000`
- [x] natapp运行并映射8080端口
- [x] `front/src/utils/request.js` 使用公网地址
- [x] `application-dev.yml` 使用公网地址
- [x] 插件 `config.js` 配置正确

测试清单：

- [ ] 插件电脑能访问 `http://192.168.1.144:3000/login?from=plugin`
- [ ] 页面显示二维码（不是JSON）
- [ ] Network面板显示API请求使用公网地址
- [ ] 扫码登录成功
- [ ] 插件收到 loginToken
- [ ] Token交换成功

---

## 🎯 立即测试

### 在插件电脑的浏览器中打开：

```
http://192.168.1.144:3000/login?from=plugin
```

### 应该看到：

1. ✅ 完整的登录页面
2. ✅ "插件登录模式"提示
3. ✅ 微信登录二维码
4. ✅ 页面背景是紫色渐变
5. ✅ 页面底部有三个功能特点展示

### 如果看到JSON错误：

说明前端还没重启成功，需要：
1. 检查前端是否在运行（3000端口）
2. 清除浏览器缓存
3. 强制刷新（Ctrl+F5）

---

## 🎉 总结

**问题**：插件电脑访问前端时，二维码链接是 `localhost:8080`，无法访问

**原因**：前端通过局域网IP访问时，vite的代理不生效

**解决**：修改 `request.js` 的默认 `baseURL` 为公网地址

**结果**：
- ✅ 插件电脑可以通过局域网IP访问前端
- ✅ 前端的API请求使用公网地址
- ✅ 二维码可以正常生成和扫描
- ✅ 插件登录流程完整可用

现在可以愉快地开发插件了！🚀

