# 填充逻辑
## 参考代码
- @js\content.js
  - 填充逻辑功能代码
- @case\performFilling.js
  - 填写逻辑
    - 步骤八
    - 步骤九
- @task\localGroups.json

## 检查
检查填充功能，是否正确使用工具函数，
如主函数从步骤六开始的：
- 匹配字段和输入框
- 查找字段对应的DOM元素
- 查找每个字段下的输入框  

具体到里面每个下拉框、多选框等的工具函数

## 目前问题
- 下拉框没有正常选择选项，只是强制填充了，一失去焦点就变成未填充状态，需要选择选项，当前代码里面有个工具函数是获取下拉选项，然后匹配，请调用那个工具函数来实现findMatchingOption
- 地区选择框，部分地区选择框是一个下拉多选的多选框，你输入内容是对下拉选项的筛选，最后通过选择来确定地区，也可能是输入框，可以判断，如果出现了下拉就点击一下最下面的选项，因为是树形结构，如：搜索深圳，则是中国/广东/深圳，选择深圳即可，如果选择广东，则广东的所有地区都会被选择，进行两段逻辑判断，实现填充功能

## 需求
上面两个个问题一直没有解决，你通过我提供的检查思路，然后通过第二个参考代码performFilling.js的填写步骤，去解决上面这三个问题。

